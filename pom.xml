<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>rsb</groupId>
	<artifactId>rsb</artifactId>
	<version>0.8.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>RSB</name>
	<url>https://code.cor-lab.org/projects/rsb/</url>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<pbuf.protopath>${env.prefix}/share/rsbprotocol</pbuf.protopath>
		<pbuf.protoc>/usr/bin/protoc</pbuf.protoc>
		<pbuf.version>2.4.1</pbuf.version>
		<pbuf.outpath>target/generated-protocol</pbuf.outpath>
		<spread.daemon>/usr/sbin/spread</spread.daemon>
	</properties>

	<build>
		<sourceDirectory>src</sourceDirectory>
		<testSourceDirectory>test</testSourceDirectory>
		<resources>
			<resource>
				<directory>src</directory>
				<excludes>
					<exclude>**/*.java</exclude>
				</excludes>
			</resource>
		</resources>

		<plugins>
			<!-- Check the availability of different external dependencies. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>1.1.1</version>
				<executions>
					<execution>
						<id>enforce-external-dependencies</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<failFast>true</failFast>
							<rules>
								<requireJavaVersion>
									<!-- This defines the minimum version. -->
									<version>1.6</version>
								</requireJavaVersion>
								<requireFilesExist>
									<message>Google Protocol Buffers compiler not found at '${pbuf.protoc}'. Please set pbuf.protoc.</message>
									<files>
										<file>${pbuf.protoc}</file>
									</files>
								</requireFilesExist>
								<requireFilesExist>
									<message>RSB protocol property pbuf.protopath does not point to a valid installation of RSBProtocol at '${pbuf.protopath}'</message>
									<files>
										<file>${pbuf.protopath}/rsb/protocol/Notification.proto</file>
									</files>
								</requireFilesExist>
								<requireFilesExist>
									<message>Spread daemon not found at '${spread.daemon}'. Please set spread.daemon.</message>
									<files>
										<file>${spread.daemon}</file>
									</files>
								</requireFilesExist>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Compiler settings -->
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<source>1.6</source>
					<target>1.6</target>
				</configuration>
			</plugin>

			<!-- Different steps realized using ant. -->
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<!-- Generate protobuf classes. -->
					<execution>
						<id>generate-protocol</id>
						<phase>generate-sources</phase>
						<configuration>
							<tasks>
								<mkdir dir="${pbuf.outpath}" />
								<apply failonerror="true" executable="${pbuf.protoc}">
									<arg value="--java_out=${pbuf.outpath}" />
									<arg value="--proto_path=${pbuf.protopath}" />
									<fileset dir="${pbuf.protopath}" includes="**/*.proto" />
								</apply>
								<apply failonerror="true" executable="${pbuf.protoc}">
									<arg value="--java_out=${pbuf.outpath}" />
									<arg value="--proto_path=${basedir}/examples/tutorial/protocol" />
									<fileset dir="examples/tutorial/protocol" includes="*.proto" />
								</apply>
							</tasks>

						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>

					<!-- Start spread before running tests. -->
					<execution>
						<id>start-spread</id>
						<phase>generate-test-resources</phase>
						<configuration>
							<tasks>
								<taskdef resource="net/sf/antcontrib/antcontrib.properties"
									classpathref="maven.plugin.classpath" />
								<if>
									<not>
										<equals arg1="${skipTests}" arg2="true" />
									</not>
									<then>
										<forget>
											<exec executable="${spread.daemon}">
												<arg line="-n localhost -c ${basedir}/etc/spread.conf" />
											</exec>
										</forget>

										<sleep seconds="10" />
									</then>
								</if>
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>ant-contrib</groupId>
						<artifactId>ant-contrib</artifactId>
						<version>1.0b3</version>
						<exclusions>
							<exclusion>
								<groupId>ant</groupId>
								<artifactId>ant</artifactId>
							</exclusion>
						</exclusions>
					</dependency>
					<dependency>
						<groupId>ant</groupId>
						<artifactId>ant-nodeps</artifactId>
						<version>1.6.5</version>
					</dependency>
				</dependencies>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>1.7</version>
				<executions>
					<execution>
						<id>add-source</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>spread-4.0.0</source>
								<source>${pbuf.outpath}</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.12.4</version>
				<configuration>
					<workingDirectory>${basedir}</workingDirectory>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.apache.maven.surefire</groupId>
						<artifactId>surefire-junit47</artifactId>
						<version>2.12.4</version>
					</dependency>
				</dependencies>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>cobertura-maven-plugin</artifactId>
				<version>2.5.2</version>
				<configuration>
					<instrumentation>
						<excludes>
							<exclude>rsb/protocol/**/*.class</exclude>
							<exclude>spread/**/*.class</exclude>
						</excludes>
					</instrumentation>
				</configuration>
				<executions>
					<execution>
						<id>clean</id>
						<phase>pre-site</phase>
						<goals>
							<goal>clean</goal>
						</goals>
					</execution>
					<execution>
						<id>instrument</id>
						<phase>site</phase>
						<goals>
							<goal>cobertura</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<reporting>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>cobertura-maven-plugin</artifactId>
				<version>2.5.2</version>
				<configuration>
					<formats>
						<format>html</format>
						<format>xml</format>
					</formats>
				</configuration>
			</plugin>
		</plugins>
	</reporting>

	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.10</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>com.google.protobuf</groupId>
			<artifactId>protobuf-java</artifactId>
			<version>${pbuf.version}</version>
		</dependency>
	</dependencies>
</project>
